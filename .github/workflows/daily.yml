name: Automated Market Analytics

on:
  schedule:
    - cron: '15 20 * * *'
  workflow_dispatch:

jobs:
  build-and-send:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4.2.2

      - name: Deploy Architecture Files
        run: |
          mkdir -p modules
          touch modules/__init__.py
          
          cat << 'EOF' > requirements.txt
          aiohttp==3.11.11
          beautifulsoup4==4.12.3
          lxml==5.1.0
          EOF
          
          cat << 'EOF' > modules/config.py
          import os
          BOT_TOKEN = os.getenv("BOT_TOKEN")
          CHAT_ID = os.getenv("CHAT_ID")
          HTTP_TIMEOUT = 15
          MAX_NEWS_PER_SECTOR = 1
          
          HEADERS = {
              "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Safari/537.36",
              "Accept": "text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8",
              "Accept-Language": "en-US,en;q=0.9,he;q=0.8",
              "Origin": "https://www.funder.co.il",
              "Referer": "https://www.funder.co.il/"
          }

          ISRAEL_FUNDS = {
              "5141841": "https://www.funder.co.il/fund/5141841",
              "5140744": "https://www.funder.co.il/fund/5140744",
              "5128624": "https://www.funder.co.il/fund/5128624",
              "5135355": "https://www.funder.co.il/fund/5135355",
              "5132337": "https://www.funder.co.il/fund/5132337",
              "5122585": "https://www.funder.co.il/fund/5122585",
              "5132642": "https://www.funder.co.il/fund/5132642",
              "5138136": "https://www.funder.co.il/fund/5138136",
              "5129481": "https://www.funder.co.il/fund/5129481",
              "5125828": "https://www.funder.co.il/fund/5125828",
              "5132535": "https://www.funder.co.il/fund/5132535",
              "5136015": "https://www.funder.co.il/fund/5136015",
              "5131420": "https://www.funder.co.il/fund/5131420",
              "5132170": "https://www.funder.co.il/fund/5132170"
          }

          SECTORS_CONFIG = {
              "Oil": {
                  "name": "Нефть и Энергия", "icon": "🛢️", "short_eng": "Oil", "news_ticker": "USO", "il_fund": "5141841", 
                  "il_name_eng": "WTI Ultra x3",
                  "tickers": ["BZ=F", "CL=F", "USO", "OVX"]
              },
              "AI": {
                  "name": "Искусственный Интеллект", "icon": "🤖", "short_eng": "AI", "news_ticker": "NVDA", "il_fund": "5140744", 
                  "il_name_eng": "Migdal AI",
                  "tickers": ["NVDA", "MSFT", "AMD", "AVGO", "PLTR", "GOOGL", "AMZN", "TSM", "BOTZ", "AIQ", "ROBO", "IRBO"]
              },
              "Gaming": {
                  "name": "Игровая Индустрия", "icon": "🎮", "short_eng": "Game", "news_ticker": "SONY", "il_fund": "5128624", 
                  "il_name_eng": "MTF Bluestar E-Games",
                  "tickers": ["NVDA", "MSFT", "TTWO", "EA", "RBLX", "SONY", "NTDOY", "UBSFY", "ESPO", "HERO", "GAMR"]
              },
              "Semiconductors": {
                  "name": "Полупроводники", "icon": "💻", "short_eng": "Semi", "news_ticker": "NVDA", "il_fund": "5135355", 
                  "il_name_eng": "Kasam Semiconductors",
                  "tickers": ["NVDA", "AMD", "AVGO", "QCOM", "TSM", "ASML", "INTC", "SOXX", "SMH"]
              },
              "Banking": {
                  "name": "Банковский сектор", "icon": "🏦", "short_eng": "Bank", "news_ticker": "JPM", "il_fund": "5132337", 
                  "il_name_eng": "Meitav Banks US",
                  "tickers": ["JPM", "BAC", "GS", "MS", "WFC", "XLF", "KBE"]
              },
              "Biotech": {
                  "name": "Биотех и Медицина", "icon": "🏥", "short_eng": "Bio", "news_ticker": "LLY", "il_fund": "5122585", 
                  "il_name_eng": "Harel Nasdaq Biotech",
                  "tickers": ["LLY", "NVO", "PFE", "MRNA", "AMGN", "XBI", "IBB"]
              },
              "Clean Energy": {
                  "name": "Зеленая энергетика", "icon": "☀️", "short_eng": "Clean", "news_ticker": "FSLR", "il_fund": "5132642", 
                  "il_name_eng": "Meitav Clean Energy",
                  "tickers": ["ICLN", "TAN", "FAN", "FSLR", "ENPH"]
              },
              "Uranium": {
                  "name": "Уран и Атомная энергетика", "icon": "☢️", "short_eng": "Uran", "news_ticker": "CCJ", "il_fund": "5138136", 
                  "il_name_eng": "More Uranium Giants",
                  "tickers": ["URA", "URNM", "CCJ", "NXE"]
              },
              "Gold": {
                  "name": "Золото и Металлы", "icon": "🥇", "short_eng": "Gold", "news_ticker": "GLD", "il_fund": "5129481", 
                  "il_name_eng": "Tachlit Gold Spot",
                  "tickers": ["GLD", "GDX", "SLV"]
              },
              "Cybersecurity": {
                  "name": "Кибербезопасность", "icon": "🛡️", "short_eng": "Cyber", "news_ticker": "CRWD", "il_fund": "5125828", 
                  "il_name_eng": "IBI Cybersecurity",
                  "tickers": ["CIBR", "HACK", "CRWD", "PANW", "FTNT", "ZS"]
              },
              "EV": {
                  "name": "Электромобили", "icon": "🚗", "short_eng": "EV", "news_ticker": "TSLA", "il_fund": "5132535", 
                  "il_name_eng": "Migdal EV & Auto",
                  "tickers": ["TSLA", "BYDDY", "RIVN", "LI", "DRIV"]
              },
              "Aerospace": {
                  "name": "Авиация и Космос", "icon": "✈️", "short_eng": "Aero", "news_ticker": "BA", "il_fund": "5136015", 
                  "il_name_eng": "Tachlit US Aerospace",
                  "tickers": ["BA", "EADSY", "LMT", "RTX", "ITA"]
              },
              "E-Commerce": {
                  "name": "Электронная коммерция", "icon": "🛒", "short_eng": "Ecom", "news_ticker": "AMZN", "il_fund": "5131420", 
                  "il_name_eng": "Meitav E-Commerce",
                  "tickers": ["AMZN", "SHOP", "MELI", "IBUY"]
              },
              "Cloud": {
                  "name": "Облачные технологии", "icon": "☁️", "short_eng": "Cloud", "news_ticker": "MSFT", "il_fund": "5132170", 
                  "il_name_eng": "Harel Cloud Comp.",
                  "tickers": ["MSFT", "AMZN", "GOOGL", "ORCL", "SNOW", "WCLD", "CLOU"]
              }
          }
          EOF
          
          cat << 'EOF' > modules/market.py
          import re, aiohttp, asyncio
          from modules.config import ISRAEL_FUNDS, HTTP_TIMEOUT, HEADERS
          
          async def fetch_single_ticker(session: aiohttp.ClientSession, ticker: str) -> tuple:
              api_ticker = f"^{ticker}" if ticker == "OVX" else ticker
              url = f"https://query1.finance.yahoo.com/v8/finance/chart/{api_ticker}?range=1d&interval=1m"
              try:
                  async with session.get(url, headers=HEADERS, timeout=HTTP_TIMEOUT) as response:
                      if response.status == 200:
                          data = await response.json()
                          result = data.get("chart", {}).get("result", [])
                          if result:
                              meta = result[0].get("meta", {})
                              price = meta.get("regularMarketPrice")
                              prev_close = meta.get("chartPreviousClose")
                              if price is not None and prev_close:
                                  change = ((price - prev_close) / prev_close) * 100
                                  return ticker, {"price": round(price, 2), "change": round(change, 2)}
              except Exception as e:
                  print(f"Ошибка тикера {ticker}: {e}")
              return ticker, {"price": 0.0, "change": 0.0}

          async def fetch_all_yahoo_data(tickers: list) -> dict:
              async with aiohttp.ClientSession() as session:
                  tasks = [fetch_single_ticker(session, t) for t in tickers]
                  results = await asyncio.gather(*tasks)
                  return dict(results)
              
          async def fetch_israeli_fund(session: aiohttp.ClientSession, fund_id: str) -> dict:
              url = ISRAEL_FUNDS.get(fund_id)
              if not url: return {"price": 0.0, "change": 0.0}
              try:
                  async with session.get(url, headers=HEADERS, timeout=HTTP_TIMEOUT) as response:
                      if response.status == 200:
                          html_content = await response.text()
                          price_match = re.search(r'(?:lblLastRate|LastRate|latest-rate)[^>]*>([\d\.,]+)', html_content, re.IGNORECASE)
                          change_match = re.search(r'(?:lblDailyChange|DailyChange|daily-change)[^>]*>([\d\.,\-+]+)%?', html_content, re.IGNORECASE)
                          if price_match:
                              price = float(price_match.group(1).replace(",", ""))
                              if price > 6000: price /= 100.0
                              raw_change = change_match.group(1).replace("+", "").replace("%", "") if change_match else "0.0"
                              return {"price": round(price, 2), "change": round(float(raw_change), 2)}
              except Exception as e:
                  print(f"Ошибка парсинга Funder ID {fund_id}: {e}")
              return {"price": 0.0, "change": 0.0}
          EOF
          
          cat << 'EOF' > modules/news.py
          import aiohttp, urllib.parse, html, re
          from modules.config import HTTP_TIMEOUT, MAX_NEWS_PER_SECTOR, HEADERS
          
          def summarize_text(text: str) -> str:
              text = re.sub(r'\s*[:—-]\s*(Yahoo Finance|Reuters|Bloomberg|CNBC|Motley Fool|MarketWatch|MT Newswires|Zacks|Barrons\.com|24/7 Wall St\.).*$', '', text, flags=re.IGNORECASE)
              text = text.strip()
              if not text.endswith('.'): text += '.'
              if len(text) > 130:
                  text = text[:127] + "..."
              return text

          async def translate_text(session: aiohttp.ClientSession, text: str) -> str:
              try:
                  url = f"https://api.mymemory.translated.net/get?q={urllib.parse.quote(text)}&langpair=en|ru"
                  async with session.get(url, timeout=5) as resp:
                      if resp.status == 200:
                          return html.unescape((await resp.json()).get("responseData", {}).get("translatedText", text))
              except: pass
              return text

          async def fetch_sector_news(session: aiohttp.ClientSession, query_ticker: str) -> list:
              url = f"https://query1.finance.yahoo.com/v1/finance/search?q={query_ticker}"
              news_list = []
              try:
                  async with session.get(url, headers=HEADERS, timeout=HTTP_TIMEOUT) as response:
                      if response.status == 200:
                          for item in (await response.json()).get("news", [])[:MAX_NEWS_PER_SECTOR]:
                              title = item.get("title", "")
                              if title:
                                  short_title = summarize_text(title)
                                  translated = await translate_text(session, short_title)
                                  news_list.append(f"• {translated}")
              except: pass
              return news_list or ["• Значимых новостей не зафиксировано."]
          EOF
          
          cat << 'EOF' > modules/formatter.py
          from datetime import datetime

          def escape_html(text: str) -> str:
              return text.replace("&", "&amp;").replace("<", "&lt;").replace(">", "&gt;")

          def get_score_emoji(score: int) -> str:
              if score >= 75: return "🟢"
              if score >= 45: return "🟡"
              return "🔴"

          def build_telegram_message(sectors_data: list) -> str:
              sorted_sectors = sorted(sectors_data, key=lambda x: x['score'], reverse=True)
              now_str = datetime.now().strftime("%d.%m.%Y | 23:15")
              
              lines = [
                  "📊 <b>MARKET ANALYTICS REPORT</b>",
                  f"📅 {now_str} 🇮🇱\n",
                  "<b>🌍 GLOBAL HEAT MAP MATRIX</b>",
              ]
              
              lines.append("<code>💼 Name     US     IL     Рейтинг</code>")
              lines.append("<code>" + "-" * 33 + "</code>")
              
              lines.append("<code>")
              for s in sorted_sectors:
                  icon = s['icon']
                  name = s['short_eng'][:6]
                  tag_emoji = get_score_emoji(s['score'])
                  
                  us_ch = s['avg_change']
                  il_ch = s['il_fund_data']['change'] 
                  
                  us_str = f"+{us_ch:.1f}%" if us_ch > 0 else f"{us_ch:.1f}%"
                  il_str = f"+{il_ch:.1f}%" if il_ch > 0 else f"{il_ch:.1f}%"
                  
                  # Если Funder вернул 0 (нет торгов), всё равно выводим число, но визуально не ломаем сетку
                  if s['il_fund_data']['price'] == 0.0 and il_ch == 0.0:
                      il_str = " 0.0%"
                  
                  score_str = f"{tag_emoji}{s['score']:>3}/100"
                  
                  row = f"{icon}{name:<6} {us_str:>5} {il_str:>5}  {score_str}"
                  lines.append(row)
              lines.append("</code>")
              
              lines.append('\n<blockquote expandable>')
              lines.append("🔍 <b>ДЕТАЛЬНАЯ АНАЛИТИКА И ДВИЖЕНИЕ АКТИВОВ</b>\n")
              
              for s in sorted_sectors:
                  ch_sign = f"+{s['avg_change']:.2f}%" if s['avg_change'] > 0 else f"{s['avg_change']:.2f}%"
                  lines.append(f"<b>{s['icon']} {s['name'].upper()}</b> ({ch_sign})")
                  
                  if s.get('top_movers'):
                      mover_tokens = []
                      for ticker, ch in s['top_movers']:
                          m_emoji = "📈" if ch >= 0 else "📉"
                          m_sign = f"+{ch:.2f}%" if ch > 0 else f"{ch:.2f}%"
                          mover_tokens.append(f"{m_emoji}{ticker}({m_sign})")
                      lines.append("  " + "  ".join(mover_tokens))
                  
                  for n in s['news']:
                      lines.append(f"  {escape_html(n)}")
                  lines.append("") 
                  
              lines.append("</blockquote>")
              
              # Обновленный нижний блок TA STOCK MARKET
              lines.append("\n🇮🇱 <b>TA STOCK MARKET (ФОНДЫ ИНДУСТРИЙ)</b>")
              lines.append("<code>" + "-" * 33 + "</code>")
              
              for s in sorted_sectors:
                  il_ch = s['il_fund_data']['change']
                  
                  # Если изменение положительное/отрицательное — стандартные зеленый/красный маркеры.
                  # Если изменение ровно 0.0 (или сайт недоступен/выходной день), выводим нейтральный белый эмодзи ⚪️ 
                  # Но подставляем саму цифру последних торгов
                  if il_ch > 0:
                      ch_emoji = "🟢"
                  elif il_ch < 0:
                      ch_emoji = "🔴"
                  else:
                      ch_emoji = "⚪"
                      
                  ch_sign = f"+{il_ch:.2f}%" if il_ch > 0 else f"{il_ch:.2f}%"
                  
                  lines.append(f"<code>{s['icon']} {s['il_name_eng']:<20} | </code>{ch_emoji} {ch_sign}")

              lines.append("\n⚡︎ ⚡︎ ⚡︎ ⚡︎ ⚡︎ ⚡︎ ⚡︎ ⚡︎ ⚡︎ ⚡︎ ⚡︎ ⚡︎ ⚡️\n")
              lines.append("@oil_tracker_pavel_bot")
              return "\n".join(lines)
          EOF
          
          cat << 'EOF' > modules/telegram_sender.py
          import aiohttp
          from modules.config import BOT_TOKEN, CHAT_ID, HTTP_TIMEOUT
          
          async def send_to_telegram(text: str):
              if not BOT_TOKEN or not CHAT_ID:
                  print("❌ Ошибка конфигурации: Отсутствуют BOT_TOKEN или CHAT_ID")
                  return
              url = f"https://api.telegram.org/bot{BOT_TOKEN}/sendMessage"
              payload = {
                  "chat_id": CHAT_ID,
                  "text": text,
                  "parse_mode": "HTML",
                  "disable_web_page_preview": True
              }
              async with aiohttp.ClientSession() as session:
                  try:
                      async with session.post(url, json=payload, timeout=HTTP_TIMEOUT) as resp:
                          resp_text = await resp.text()
                          if resp.status == 200:
                              print("🚀 Сообщение успешно отправлено в Telegram!")
                          else:
                              print(f"❌ Ошибка Telegram API (Статус {resp.status}): {resp_text}")
                  except Exception as e: 
                      print(f"💥 Критическая сетевая ошибка при отправке: {e}")
          EOF
          
          cat << 'EOF' > main.py
          import asyncio, aiohttp
          from modules.config import SECTORS_CONFIG
          from modules.market import fetch_all_yahoo_data, fetch_israeli_fund
          from modules.news import fetch_sector_news
          from modules.formatter import build_telegram_message
          from modules.telegram_sender import send_to_telegram

          def calculate_sector_score(avg_change: float) -> int:
              base_score = 55
              score = int(base_score + (avg_change * 16.5))
              return max(0, min(100, score))

          async def main():
              all_tickers = set()
              for cfg in SECTORS_CONFIG.values():
                  all_tickers.update(cfg["tickers"])
              
              print(f"📦 Сбор рыночных данных для {len(all_tickers)} уникальных тикеров...")
              global_market_data = await fetch_all_yahoo_data(list(all_tickers))
              
              async with aiohttp.ClientSession() as session:
                  sectors_results = []
                  
                  il_tasks = {}
                  for key, cfg in SECTORS_CONFIG.items():
                      if cfg["il_fund"]:
                          il_tasks[key] = fetch_israeli_fund(session, cfg["il_fund"])
                  
                  il_results = {}
                  if il_tasks:
                      il_keys = list(il_tasks.keys())
                      il_res_data = await asyncio.gather(*il_tasks.values())
                      il_results = dict(zip(il_keys, il_res_data))
                  
                  for key, cfg in SECTORS_CONFIG.items():
                      valid_changes = []
                      ticker_movers = []
                      
                      for t in cfg["tickers"]:
                          if t in global_market_data and global_market_data[t]["price"] > 0:
                              ch_val = global_market_data[t]["change"]
                              valid_changes.append(ch_val)
                              ticker_movers.append((t, ch_val))
                      
                      avg_change = sum(valid_changes) / len(valid_changes) if valid_changes else 0.0
                      score = calculate_sector_score(avg_change)
                      news = await fetch_sector_news(session, cfg["news_ticker"])
                      
                      top_movers = []
                      if ticker_movers:
                          sorted_movers = sorted(ticker_movers, key=lambda x: x[1], reverse=True)
                          top_movers.append(sorted_movers[0]) 
                          if len(sorted_movers) > 1:
                              top_movers.append(sorted_movers[-1])
                      
                      sectors_results.append({
                          "name_eng": key,
                          "short_eng": cfg["short_eng"],
                          "name": cfg["name"],
                          "icon": cfg["icon"],
                          "avg_change": round(avg_change, 2),
                          "score": score,
                          "top_movers": top_movers,
                          "news": news,
                          "il_id": cfg["il_fund"],
                          "il_name_eng": cfg["il_name_eng"],
                          "il_fund_data": il_results.get(key, {"price": 0.0, "change": 0.0})
                      })
                      
                  message = build_telegram_message(sectors_results)
                  await send_to_telegram(message)

          if __name__ == "__main__":
              asyncio.run(main())
          EOF

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Analytics Engine
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: python main.py
