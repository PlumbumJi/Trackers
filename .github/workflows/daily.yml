name: Automated Market Analytics

on:
  schedule:
    # Пн-Пт в 23:10 по Израилю (20:10 UTC) — Рыночная аналитика
    - cron: '10 20 * * 1-5'
    # Суббота в 22:00 по Израилю (19:00 UTC) — IPO отчет
    - cron: '0 19 * * 6'
  workflow_dispatch:

jobs:
  build-and-send:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4.2.2

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Deploy Architecture Files
        run: |
          mkdir -p modules
          touch modules/__init__.py
          
          cat << 'EOF' > requirements.txt
          aiohttp==3.11.11
          beautifulsoup4==4.12.3
          EOF
          
          cat << 'EOF' > modules/config.py
          import os
          BOT_TOKEN = os.getenv("BOT_TOKEN")
          CHAT_ID = os.getenv("CHAT_ID")
          HTTP_TIMEOUT = 12
          BOT_NAME = "@oil_tracker_pavel_bot"
          
          HEADERS = {
              "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Safari/537.36",
              "Accept": "text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webkit,javascript,json,*/*;q=0.8",
              "Accept-Language": "he-IL,he;q=0.9,en-US;q=0.8,en;q=0.7",
              "Cache-Control": "no-cache",
              "Pragma": "no-cache"
          }

          SECTORS_CONFIG = {
              "Oil": {
                  "name": "Нефть и Энергетика", "icon": "🛢️", "short_eng": "Oil", "news_ticker": "USO", 
                  "fund_id": "5141841", "il_name_eng": "WTI Ultra x3", 
                  "tickers": ["BZ=F", "CL=F", "USO", "OVX", "XLE", "VLO"]
              },
              "Cybersecurity": {
                  "name": "Кибербезопасность", "icon": "🛡️", "short_eng": "Cyber", "news_ticker": "CIBR", 
                  "fund_id": "5140744", "il_name_eng": "IBI Cybersecurity", 
                  "tickers": ["CIBR", "HACK", "CRWD", "PANW", "FTNT", "ZS", "NET"]
              },
              "Gold": {
                  "name": "Золото и Металлы", "icon": "🥇", "short_eng": "Gold", "news_ticker": "GLD", 
                  "fund_id": "5128624", "il_name_eng": "Tachlit Gold Spot", 
                  "tickers": ["GLD", "GDX", "SLV", "NEM", "GOLD"]
              },
              "Clean Energy": {
                  "name": "Зеленая энергетика", "icon": "☀️", "short_eng": "Clean", "news_ticker": "FSLR", 
                  "fund_id": "5138136", "il_name_eng": "Meitav Clean Energy", 
                  "tickers": ["ICLN", "TAN", "FAN", "FSLR", "ENPH", "SEDG"]
              },
              "Aerospace": {
                  "name": "Авиация и Космос", "icon": "✈️", "short_eng": "Aero", "news_ticker": "ITA", 
                  "fund_id": "5135116", "il_name_eng": "Tachlit US Aerospace", 
                  "tickers": ["BA", "EADSY", "LMT", "RTX", "ITA", "NOC"]
              },
              "Biotech": {
                  "name": "Биотех и Медицина", "icon": "🏥", "short_eng": "Bio", "news_ticker": "LLY", 
                  "fund_id": "5122965", "il_name_eng": "Harel Nasdaq Biotech", 
                  "tickers": ["LLY", "NVO", "PFE", "MRNA", "AMGN", "XBI", "IBB"]
              },
              "Cloud": {
                  "name": "Облачные технологии", "icon": "☁️", "short_eng": "Cloud", "news_ticker": "WCLD", 
                  "fund_id": "5131014", "il_name_eng": "Harel Cloud Comp.", 
                  "tickers": ["MSFT", "AMZN", "GOOGL", "ORCL", "SNOW", "WCLD", "CLOU"]
              },
              "Banking": {
                  "name": "Банковский сектор", "icon": "🏦", "short_eng": "Bank", "news_ticker": "JPM", 
                  "fund_id": "5138144", "il_name_eng": "Meitav Banks US", 
                  "tickers": ["JPM", "BAC", "GS", "MS", "WFC", "XLF", "KBE"]
              },
              "Uranium": {
                  "name": "Уран и Атомная энергетика", "icon": "☢️", "short_eng": "Uran", "news_ticker": "CCJ", 
                  "fund_id": "5138763", "il_name_eng": "More Uranium Giants", 
                  "tickers": ["URA", "URNM", "CCJ", "NXE", "DNN"]
              },
              "Gaming": {
                  "name": "Игровая Индустрия", "icon": "🎮", "short_eng": "Game", "news_ticker": "SONY", 
                  "fund_id": "5130198", "il_name_eng": "MTF Bluestar E-Games", 
                  "tickers": ["NVDA", "MSFT", "TTWO", "EA", "RBLX", "SONY", "NTDOY", "HERO"]
              },
              "EV": {
                  "name": "Электромобили", "icon": "🚗", "short_eng": "EV", "news_ticker": "TSLA", 
                  "fund_id": "5132228", "il_name_eng": "Migdal EV & Auto", 
                  "tickers": ["TSLA", "BYDDY", "RIVN", "LI", "DRIV", "LCID"]
              },
              "E-Commerce": {
                  "name": "Электронная коммерция", "icon": "🛒", "short_eng": "Ecom", "news_ticker": "IBUY", 
                  "fund_id": "5138128", "il_name_eng": "Meitav E-Commerce", 
                  "tickers": ["AMZN", "SHOP", "MELI", "IBUY", "EBAY", "PDD"]
              },
              "Semiconductors": {
                  "name": "Полупроводники", "icon": "💻", "short_eng": "Semi", "news_ticker": "SOXX", 
                  "fund_id": "5118716", "il_name_eng": "Kasam Semiconductors", 
                  "tickers": ["NVDA", "AMD", "AVGO", "QCOM", "TSM", "ASML", "INTC", "SOXX"]
              },
              "AI": {
                  "name": "Искусственный Интеллект", "icon": "🤖", "short_eng": "AI", "news_ticker": "NVDA", 
                  "fund_id": "5134101", "il_name_eng": "Migdal AI", 
                  "tickers": ["NVDA", "MSFT", "AMD", "AVGO", "PLTR", "GOOGL", "AMZN", "BOTZ"]
              }
          }
          EOF
          
          cat << 'EOF' > modules/market.py
          import aiohttp
          import asyncio
          import re
          from bs4 import BeautifulSoup
          from modules.config import HTTP_TIMEOUT, HEADERS

          async def fetch_single_ticker(session: aiohttp.ClientSession, ticker: str) -> tuple:
              api_ticker = f"^{ticker}" if ticker == "OVX" else ticker
              url = f"https://query1.finance.yahoo.com/v8/finance/chart/{api_ticker}?range=1d&interval=1m"
              try:
                  async with session.get(url, headers=HEADERS, timeout=HTTP_TIMEOUT) as response:
                      if response.status == 200:
                          data = await response.json()
                          result = data.get("chart", {}).get("result", [])
                          if result:
                              meta = result[0].get("meta", {})
                              price = meta.get("regularMarketPrice")
                              prev_close = meta.get("chartPreviousClose")
                              if price is not None and prev_close:
                                  change = ((price - prev_close) / prev_close) * 100
                                  return ticker, {"price": round(price, 2), "change": round(change, 2)}
              except Exception:
                  pass
              return ticker, {"price": 0.0, "change": 0.0}

          async def fetch_all_yahoo_data(tickers: list) -> dict:
              async with aiohttp.ClientSession() as session:
                  tasks = [fetch_single_ticker(session, t) for t in tickers]
                  results = await asyncio.gather(*tasks)
                  return dict(results)

          def parse_float(val) -> float | None:
              if val is None:
                  return None
              try:
                  cleaned = str(val).replace(',', '.').replace('%', '').replace('+', '').strip()
                  match = re.search(r'([+-]?\d+(?:\.\d+)?)', cleaned)
                  if match:
                      res = float(match.group(1))
                      if abs(res) <= 50.0:
                          return round(res, 2)
              except Exception:
                  pass
              return None

          async def fetch_from_funder(session: aiohttp.ClientSession, fund_id: str) -> float | None:
              urls = [
                  f"https://www.funder.co.il/fund/{fund_id}",
                  f"https://www.funder.co.il/api/fund/{fund_id}"
              ]
              funder_headers = {
                  **HEADERS,
                  "Referer": "https://www.funder.co.il/",
                  "Sec-Fetch-Dest": "document",
                  "Sec-Fetch-Mode": "navigate"
              }
              for url in urls:
                  try:
                      async with session.get(url, headers=funder_headers, timeout=HTTP_TIMEOUT) as resp:
                          if resp.status == 200:
                              if "api" in url:
                                  data = await resp.json()
                                  val = parse_float(data.get("dayChange") or data.get("change"))
                              else:
                                  html_text = await resp.text()
                                  soup = BeautifulSoup(html_text, 'html.parser')
                                  elem = soup.find(id=re.compile(r'dayChange|change', re.I)) or soup.find('span', class_=re.compile(r'change|profit', re.I))
                                  val = parse_float(elem.text) if elem else None
                              if val is not None:
                                  return val
                  except Exception:
                      continue
              return None

          async def fetch_from_bizportal(session: aiohttp.ClientSession, fund_id: str) -> float | None:
              url_api = f"https://www.bizportal.co.il/api/mutualfunds/getgeneralview/{fund_id}"
              url_page = f"https://www.bizportal.co.il/mutualfunds/quote/generalview/{fund_id}"
              
              req_headers = {
                  **HEADERS,
                  "Referer": url_page,
                  "X-Requested-With": "XMLHttpRequest"
              }
              try:
                  async with session.get(url_api, headers=req_headers, timeout=HTTP_TIMEOUT) as resp:
                      if resp.status == 200:
                          data = await resp.json()
                          chg = data.get("ChangeRate") or data.get("DailyChange") or data.get("Change")
                          val = parse_float(chg)
                          if val is not None:
                              return val
              except Exception:
                  pass

              try:
                  async with session.get(url_page, headers=HEADERS, timeout=HTTP_TIMEOUT) as resp:
                      if resp.status == 200:
                          html_text = await resp.text()
                          match = re.search(r'class="[^\"]*change[^\"]*"[^>]*>([+-]?\d+(?:\.\d+)?%?)', html_text, re.I)
                          if match:
                              return parse_float(match.group(1))
              except Exception:
                  pass
              return None

          async def fetch_from_globes(session: aiohttp.ClientSession, fund_id: str) -> float | None:
              url = f"https://www.globes.co.il/portal/instrument.aspx?instrumentid={fund_id}"
              globes_headers = {
                  **HEADERS,
                  "Referer": "https://www.globes.co.il/"
              }
              try:
                  async with session.get(url, headers=globes_headers, timeout=HTTP_TIMEOUT) as resp:
                      if resp.status == 200:
                          html_text = await resp.text()
                          soup = BeautifulSoup(html_text, 'html.parser')
                          elem = soup.find('span', {'id': re.compile(r'lblChange|LastRate', re.I)}) or soup.find('td', class_=re.compile(r'change', re.I))
                          if elem:
                              return parse_float(elem.text)
              except Exception:
                  pass
              return None

          async def fetch_fund_data_by_id(session: aiohttp.ClientSession, fund_id: str) -> float:
              if not fund_id:
                  return 0.0

              tasks = [
                  asyncio.create_task(fetch_from_funder(session, fund_id)),
                  asyncio.create_task(fetch_from_bizportal(session, fund_id)),
                  asyncio.create_task(fetch_from_globes(session, fund_id))
              ]

              try:
                  for completed in asyncio.as_completed(tasks, timeout=HTTP_TIMEOUT):
                      res = await completed
                      if res is not None:
                          for t in tasks:
                              if not t.done():
                                  t.cancel()
                          print(f"✅ [{fund_id}]: {res}%")
                          return res
              except Exception:
                  pass

              print(f"⚠️ [0.00%] Не удалось извлечь данные для фонда [{fund_id}] со всех источников")
              return 0.0

          async def fetch_all_israeli_funds(sectors_config: dict) -> dict:
              results = {}
              async with aiohttp.ClientSession() as session:
                  tasks = {
                      key: fetch_fund_data_by_id(session, cfg.get("fund_id", ""))
                      for key, cfg in sectors_config.items()
                  }
                  for key, task in tasks.items():
                      val = await task
                      results[key] = {"price": 0.0, "change": val}
              return results
          EOF
          
          cat << 'EOF' > modules/news.py
          import aiohttp, urllib.parse, html
          from modules.config import HTTP_TIMEOUT, HEADERS

          async def translate_to_russian(session: aiohttp.ClientSession, text: str) -> str:
              if not text:
                  return text
              try:
                  url = f"https://translate.googleapis.com/translate_a/single?client=gtx&sl=auto&tl=ru&dt=t&q={urllib.parse.quote(text)}"
                  async with session.get(url, timeout=6) as resp:
                      if resp.status == 200:
                          data = await resp.json()
                          translated = "".join([segment[0] for segment in data[0] if segment[0]])
                          return html.unescape(translated)
              except Exception as e:
                  print(f"Ошибка перевода: {e}")
              return text

          async def fetch_sector_news(session: aiohttp.ClientSession, query_ticker: str) -> list:
              url = f"https://query1.finance.yahoo.com/v1/finance/search?q={query_ticker}"
              try:
                  async with session.get(url, headers=HEADERS, timeout=HTTP_TIMEOUT) as response:
                      if response.status == 200:
                          items = (await response.json()).get("news", [])
                          for item in items:
                              title = item.get("title", "")
                              if title and len(title) > 15:
                                  translated_title = await translate_to_russian(session, title)
                                  return [f"• {translated_title}"]
              except: pass
              return ["• Торгуется в рамках текущей рыночной динамики."]

          async def fetch_ipo_news(session: aiohttp.ClientSession) -> list:
              url = "https://query1.finance.yahoo.com/v1/finance/search?q=IPO"
              ipo_news = []
              try:
                  async with session.get(url, headers=HEADERS, timeout=HTTP_TIMEOUT) as response:
                      if response.status == 200:
                          items = (await response.json()).get("news", [])
                          for item in items[:5]:
                              title = item.get("title", "")
                              if title:
                                  translated_title = await translate_to_russian(session, title)
                                  ipo_news.append(f"📌 {translated_title}")
              except: pass
              return ipo_news or ["📌 Публикаций по IPO на этой неделе не зафиксировано."]
          EOF
          
          cat << 'EOF' > modules/formatter.py
          from datetime import datetime, timezone, timedelta
          from modules.config import BOT_NAME

          def escape_html(text: str) -> str:
              return text.replace("&", "&amp;").replace("<", "&lt;").replace(">", "&gt;")

          def build_telegram_message(sectors_data: list) -> str:
              tz_il = timezone(timedelta(hours=3))
              now_str = datetime.now(tz_il).strftime("%d.%m.%Y | %H:%M")
              
              lines = [
                  "📈 <b>Рыночный Дайджест</b>",
                  f"📅 {now_str} (IDT) 🇮🇱\n",
                  "<b>📊 ОБЗОР СЕКТОРОВ (US) И ФОНДОВ (IL)</b>",
                  "<code>Сектор       US      IL   Рейтинг</code>",
                  "<code>"
              ]
              
              for s in sectors_data:
                  icon = s['icon']
                  name = s['short_eng'][:6]
                  us_ch = s['avg_change']
                  il_ch = s['il_fund_data']['change']
                  
                  us_str = f"+{us_ch:.1f}%" if us_ch > 0 else f"{us_ch:.1f}%"
                  il_str = f"+{il_ch:.1f}%" if il_ch > 0 else f"{il_ch:.1f}%"
                  
                  score_emoji = "🟢" if s['score'] >= 65 else ("🟡" if s['score'] >= 40 else "🔴")
                  lines.append(f"{icon}{name:<6} {us_str:>6} {il_str:>6}  {score_emoji}{s['score']:>3}/100")
              lines.append("</code>\n")

              lines.append("<blockquote expandable>")
              lines.append("🔍 <b>ДРАЙВЕРЫ ДВИЖЕНИЯ И ПОКАЗАТЕЛИ СЕКТОРОВ</b>\n")
              for s in sectors_data:
                  ch_sign = f"+{s['avg_change']:.2f}%" if s['avg_change'] > 0 else f"{s['avg_change']:.2f}%"
                  lines.append(f"<b>{s['icon']} {escape_html(s['name'])}</b> ({ch_sign})")
                  
                  if s.get("top_3_tickers"):
                      details_str = ", ".join([f"{t}: {c:+.2f}%" for t, c in s["top_3_tickers"]])
                      lines.append(f"  📊 <i>Топ-3 драйвера: [{escape_html(details_str)}]</i>")
                      
                  for n in s['news']:
                      lines.append(f"  {escape_html(n)}")
                  lines.append("")
              lines.append("</blockquote>\n")

              lines.append("<blockquote expandable>")
              lines.append("🇮🇱 <b>TA Stocks</b>\n")
              for s in sectors_data:
                  il_ch = s['il_fund_data']['change']
                  ch_emoji = "🟢" if il_ch > 0 else ("🔴" if il_ch < 0 else "⚪")
                  ch_sign = f"+{il_ch:.2f}%" if il_ch > 0 else f"{il_ch:.2f}%"
                  lines.append(f"{ch_emoji} <b>{escape_html(s['il_name_eng'])}</b> ({s['icon']})\n└ <code>{s['fund_id']}</code> · <b>{ch_sign}</b>")
              lines.append("</blockquote>")

              lines.append(f"\n{BOT_NAME}")
              return "\n".join(lines)

          def build_ipo_message(ipo_news: list) -> str:
              tz_il = timezone(timedelta(hours=3))
              now_str = datetime.now(tz_il).strftime("%d.%m.%Y")
              
              lines = [
                  "🔔 <b>ЕЖЕНЕДЕЛЬНЫЙ IPO ДАЙДЖЕСТ</b>",
                  f"📅 {now_str} | Субботний обзор 🇮🇱\n",
                  "<blockquote expandable>"
              ]
              for news in ipo_news:
                  lines.append(f"{escape_html(news)}\n")
              lines.append("</blockquote>")
              lines.append(f"\n{BOT_NAME}")
              return "\n".join(lines)
          EOF
          
          cat << 'EOF' > modules/telegram_sender.py
          import aiohttp
          from modules.config import BOT_TOKEN, CHAT_ID, HTTP_TIMEOUT

          def split_message(text: str, max_length: int = 4000) -> list[str]:
              if len(text) <= max_length:
                  return [text]
              
              chunks = []
              raw_blocks = text.split("</blockquote>")
              current_chunk = ""

              for raw_block in raw_blocks:
                  if not raw_block:
                      continue
                  
                  block = raw_block + "</blockquote>" if "blockquote" in raw_block else raw_block

                  if len(current_chunk) + len(block) > max_length:
                      if current_chunk:
                          chunks.append(current_chunk.strip())
                          current_chunk = ""
                      
                      if len(block) > max_length:
                          lines = block.split("\n")
                          for line in lines:
                              if len(current_chunk) + len(line) + 1 > max_length:
                                  chunks.append(current_chunk.strip())
                                  current_chunk = line + "\n"
                              else:
                                  current_chunk += line + "\n"
                      else:
                          current_chunk = block
                  else:
                      current_chunk += block

              if current_chunk.strip():
                  chunks.append(current_chunk.strip())

              return chunks

          async def send_to_telegram(text: str):
              if not BOT_TOKEN or not CHAT_ID:
                  print("❌ BOT_TOKEN или CHAT_ID не найдены")
                  return

              url = f"https://api.telegram.org/bot{BOT_TOKEN}/sendMessage"
              chunks = split_message(text, max_length=3900)
              
              print(f"ℹ️ Общая длина сообщения: {len(text)} символов. Итоговых частей: {len(chunks)}")
              
              async with aiohttp.ClientSession() as session:
                  for idx, chunk in enumerate(chunks, start=1):
                      payload = {
                          "chat_id": CHAT_ID, 
                          "text": chunk, 
                          "parse_mode": "HTML", 
                          "disable_web_page_preview": True
                      }
                      try:
                          async with session.post(url, json=payload, timeout=HTTP_TIMEOUT) as resp:
                              if resp.status == 200:
                                  print(f"🚀 Часть [{idx}/{len(chunks)}] ({len(chunk)} симв.) успешно отправлена!")
                              else:
                                  print(f"❌ Ошибка Telegram (код {resp.status}): {await resp.text()}")
                      except Exception as e:
                          print(f"💥 Ошибка отправки: {e}")
          EOF
          
          cat << 'EOF' > main.py
          import asyncio, aiohttp
          from datetime import datetime, timezone, timedelta
          from modules.config import SECTORS_CONFIG
          from modules.market import fetch_all_yahoo_data, fetch_all_israeli_funds
          from modules.news import fetch_sector_news, fetch_ipo_news
          from modules.formatter import build_telegram_message, build_ipo_message
          from modules.telegram_sender import send_to_telegram

          def calculate_sector_score(avg_change: float) -> int:
              return max(0, min(100, int(50 + (avg_change * 15.0))))

          async def run_market_analytics():
              all_tickers = set()
              for cfg in SECTORS_CONFIG.values():
                  all_tickers.update(cfg["tickers"])
              
              print(f"📦 Сбор данных Yahoo Finance...")
              global_market_data = await fetch_all_yahoo_data(list(all_tickers))

              print(f"🌐 Опрос API-эндпоинтов израильских фондов...")
              il_results = await fetch_all_israeli_funds(SECTORS_CONFIG)

              async with aiohttp.ClientSession() as session:
                  sectors_results = []
                  
                  for key, cfg in SECTORS_CONFIG.items():
                      ticker_details = [(t, global_market_data[t]["change"]) for t in cfg["tickers"] if t in global_market_data and global_market_data[t]["price"] > 0]
                      
                      sorted_tickers = sorted(ticker_details, key=lambda x: abs(x[1]), reverse=True)
                      top_3_tickers = sorted_tickers[:3]
                      
                      valid_changes = [c for _, c in ticker_details]
                      avg_change = sum(valid_changes) / len(valid_changes) if valid_changes else 0.0
                      
                      score = calculate_sector_score(avg_change)
                      news = await fetch_sector_news(session, cfg["news_ticker"])
                      
                      sectors_results.append({
                          "short_eng": cfg["short_eng"],
                          "name": cfg["name"],
                          "icon": cfg["icon"],
                          "fund_id": cfg.get("fund_id", ""),
                          "avg_change": round(avg_change, 2),
                          "top_3_tickers": top_3_tickers,
                          "score": score,
                          "news": news,
                          "il_name_eng": cfg["il_name_eng"],
                          "il_fund_data": il_results.get(key, {"price": 0.0, "change": 0.0})
                      })
                  
                  # Сортировка от большего к меньшему
                  sectors_results.sort(key=lambda x: x["avg_change"], reverse=True)
                      
                  message = build_telegram_message(sectors_results)
                  await send_to_telegram(message)

          async def run_ipo_digest():
              async with aiohttp.ClientSession() as session:
                  ipo_news = await fetch_ipo_news(session)
                  message = build_ipo_message(ipo_news)
                  await send_to_telegram(message)

          async def main():
              tz_il = timezone(timedelta(hours=3))
              weekday = datetime.now(tz_il).weekday()
              
              if weekday == 5:
                  await run_ipo_digest()
              else:
                  await run_market_analytics()

          if __name__ == "__main__":
              asyncio.run(main())
          EOF

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Analytics Engine
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: python main.py
